[workspace]
members = ["contract", "aggregator"]
exclude = ["api", "indexer", "airtable-sync", "time-metrics"]
resolver = "2"

[profile.release]
codegen-units = 1
# Tell `rustc` to optimize for small code size.
opt-level = "s"
lto = true
debug = false
panic = "abort"
overflow-checks = true
